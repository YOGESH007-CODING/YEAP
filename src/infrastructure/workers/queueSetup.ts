/*
 * Redis/BullMQ queue setup is disabled for the Vercel deployment.
 *
 * This project no longer imports, builds, or starts this module. Restore the
 * previous Redis configuration only when deploying a persistent worker service.
 */
